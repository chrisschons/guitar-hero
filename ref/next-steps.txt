there are some issues with our chord diagram display logic, i'd like to adjust the arrays to simplify things 

- chord box should be fixed size with 5 columns
- column 0: mute notes and open notes
- if column one = fret 1, display the thick left border for the nut
- fret array should be relative to startFret:
E major [0,0,2,3,3,0], startFret = 0 (default, prop not needed)
B major [0,2,2,2,0,-1], startFret = 2

- optional barre property to define for ui, example f chord, barre is fret 1, string 1-6 , property would be barre:[1,1,6]


refactor

- all note visualizations should be based on math, not arrays of specific notes
- major scale pattern: w w h w w w h, minor w h w w h w w
- chords can stay roughly same, just clearer rules for the chord diagrams so they display properly


features
- need more freeform scroller that supports up to 8 divisions per beat
- need to support tunings: e, e,  drop c
- themes, change the core colors, variables across app to define ui color: background, stringColor, activeNoteColor, fullRangeNoteColor, etc
- need time signatures 4/4 3/4 6/8 12/8 etc
- riff library, small bits of note sequences, randomly mix together
- chord progressions for riff exercises
- chord changing exercises
- ideally i'd like 
- tuner, can we do that with mic access?
