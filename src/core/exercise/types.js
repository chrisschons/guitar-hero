/**
 * Exercise engine types.
 * Tab = array of columns; each column = 6 elements (fret or null per string).
 */

/** @typedef {(number | null)[]} TabColumn - length 6, index 0 = high e, 5 = low E */

/** @typedef {TabColumn[]} Tab */
